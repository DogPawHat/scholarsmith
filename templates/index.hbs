<!DOCTYPE html>
<html lang="en">
    <meta charset="utf-8"/>
    <title>{{ title }}</title>
    <script src="bundle.js" />
<body>
    <h1>WELCOME</h1>
    {{#each topics}}
        {{> topic}}
    {{/each}}
    {{> tutorialtest}}
    {{> talktous}}
</body>
</html>
