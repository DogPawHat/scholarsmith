<!DOCTYPE html>
<html lang="en">
    <title>{{ title }}</title>
<body>
    <h1>WELCOME</h1>
    {{#each topics}}
        {{> topic}}
    {{/each}}
    {{> tutorialtest}}
    {{> talktous}}
</body>
</html>
